@model WebApplication4.Models.Movie

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>


    <legend>Movie</legend>

    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.BoxOffice)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.BoxOffice)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Description)
        </dd>
        </dl>


@foreach (var item in Model.Actors)
{
    <div class="row">
        <div class="col-md-6" >
            <h3>@item.Actor.Name </h3>          <h2>@item.ScreenName </h2>
        </div>
    </div>
                        

        
        }
        <div class="row">
            <div class="col-md-6" id="Div1">

            </div>
        </div>
        <p>
            @Html.ActionLink("Edit", "Edit", new { id = Model.MovieId }, new { @class = "btn btn-primary" }) |
            @Html.ActionLink("Back to List", "Index",null, new { @class = "btn btn-primary" })
            <span class="btn btn-primary" onclick="createScreenName()">Add a Screen Name</span>

        </p>


        @section scripts
{

            <script>
                function createScreenName() {
                    $("#Div1").load('@Url.Action("Create","ScreenName")').show();
                }
            </script>
        }
